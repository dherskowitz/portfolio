<html lang="en" class="h-full">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,600;9..144,700&family=Space+Grotesk:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <title>Daniel Herskowitz | Software Developer</title>
  </head>
  <body class="bg-slate-100 text-slate-900 h-full">
    <slot />
  </body>
  <style>
    :root {
      --ink: #0f172a;
      --surface: #f6f1e8;
      --surface-2: #fff7e6;
      --tile-dark: #1a2434;
      --tile-mid: #25324a;
      --accent: #f59e0b;
      --accent-2: #0f766e;
      --accent-3: #0284c7;
      --line: rgba(15, 23, 42, 0.2);
    }

    body {
      font-family: 'Space Grotesk', 'Helvetica Neue', Arial, sans-serif;
      color: var(--ink);
      background-image:
        radial-gradient(
          1000px circle at 12% 10%,
          rgba(245, 158, 11, 0.2),
          transparent 55%
        ),
        radial-gradient(
          900px circle at 88% 18%,
          rgba(2, 132, 199, 0.18),
          transparent 50%
        ),
        linear-gradient(120deg, #f7f1e6 0%, #efe8db 45%, #f9f4ec 100%);
    }

    .font-display {
      font-family: 'Fraunces', 'Times New Roman', serif;
      letter-spacing: 0.01em;
    }

    .page-fade {
      animation: pageFade 700ms ease-out both;
    }

    .reveal {
      animation: revealUp 650ms cubic-bezier(0.21, 1, 0.32, 1) both;
    }

    .reveal-delay-1 {
      animation-delay: 90ms;
    }

    .reveal-delay-2 {
      animation-delay: 180ms;
    }

    .reveal-delay-3 {
      animation-delay: 260ms;
    }

    .reveal-delay-4 {
      animation-delay: 340ms;
    }

    @keyframes pageFade {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    @keyframes revealUp {
      from {
        opacity: 0;
        transform: translateY(18px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .page-fade,
      .reveal {
        animation: none;
      }
    }
  </style>
</html>
